# завдання 4

# Заданий список слів
список_слів = ["AbC", "aAa", "123", "AbBa", "AbbA", "слово", "tSsu", "Stus", "університет", "університет", "привіт", "світ", "Пайтон", "пайТон"]

# 4.1 Вивести слова, в яких прописних літер більше, ніж рядкових, і кількість літер така ж, як кількість цифр.
результат_4_1 = [слово for слово in список_слів if sum(1 for літера in слово if літера.isupper()) > sum(1 for літера in слово if літера.islower()) and sum(1 for літера in слово if літера.isdigit()) == len(слово)]
print("4.1:", результат_4_1)

# 4.2 Підрахунок кількості абревіатур
кількість_абревіатур = sum(1 for слово in список_слів if len(слово) > 1 and all(літера.isupper() for літера in слово))
print("4.2: Кількість абревіатур -", кількість_абревіатур)

# 4.3 Вивести слова, в яких якийсь символ зустрічається більше ніж один раз.
результат_4_3 = [слово for слово in список_слів if any(слово.count(символ) > 1 for символ in слово)]
print("4.3:", результат_4_3)

# 4.4 Визначити всі символьні конструкції, які є однаковими словами з єдиною припустимою відмінністю регістром однакових літер.
результат_4_4 = []
for i, слово1 in enumerate(список_слів):
    for слово2 in список_слів[i + 1:]:
        if слово1.lower() == слово2.lower():
            результат_4_4.append((слово1, слово2))
print("4.4:", результат_4_4)

# 4.5 Визначити всі слова, які складаються з одних і тих самих літер одного регістру, які можуть бути отримані перестановкою літер.
результат_4_5 = []
for i, слово1 in enumerate(список_слів):
    for слово2 in список_слів[i + 1:]:
        if sorted(слово1.lower()) == sorted(слово2.lower()):
            результат_4_5.append((слово1, слово2))
print("4.5:", результат_4_5)
